# Nagarik Connect - Project Architecture

## 🏗️ System Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                         CLIENT LAYER                             │
├─────────────────────────────────────────────────────────────────┤
│                                                                   │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐          │
│  │   Browser    │  │   Mobile     │  │   Tablet     │          │
│  │   (Chrome,   │  │   Browser    │  │   Browser    │          │
│  │   Firefox)   │  │              │  │              │          │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘          │
│         │                 │                 │                    │
│         └─────────────────┴─────────────────┘                    │
│                           │                                       │
│                    HTTP/HTTPS Requests                           │
│                           │                                       │
└───────────────────────────┼───────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│                      APPLICATION LAYER                           │
├─────────────────────────────────────────────────────────────────┤
│                                                                   │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │              Flask Web Application (app.py)                 │ │
│  │                                                              │ │
│  │  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐     │ │
│  │  │   Routes     │  │  Templates   │  │   Static     │     │ │
│  │  │   Handler    │  │   (Jinja2)   │  │   Files      │     │ │
│  │  └──────┬───────┘  └──────┬───────┘  └──────────────┘     │ │
│  │         │                 │                                 │ │
│  │         ▼                 ▼                                 │ │
│  │  ┌─────────────────────────────────────────────┐           │ │
│  │  │         Session Management                   │           │ │
│  │  │  (User Auth, Role-based Access Control)     │           │ │
│  │  └─────────────────────────────────────────────┘           │ │
│  └────────────────────────────────────────────────────────────┘ │
│                                                                   │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │                    API Endpoints                            │ │
│  │                                                              │ │
│  │  /api/stats          - Get complaint statistics            │ │
│  │  /submit_complaint   - Submit new complaint                │ │
│  │  /assign_worker      - Assign worker to complaint          │ │
│  │  /update_status      - Update complaint status             │ │
│  └────────────────────────────────────────────────────────────┘ │
│                                                                   │
└───────────────────────────┬───────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│                      BUSINESS LOGIC LAYER                        │
├─────────────────────────────────────────────────────────────────┤
│                                                                   │
│  ┌──────────────────┐  ┌──────────────────┐  ┌───────────────┐ │
│  │   ML Module      │  │   Translations   │  │   Config      │ │
│  │   (ml/router.py) │  │   (i18n)         │  │   (config.py) │ │
│  │                  │  │                  │  │               │ │
│  │  • Dept Predict  │  │  • English       │  │  • Settings   │ │
│  │  • Worker Match  │  │  • Hindi         │  │  • Secrets    │ │
│  │  • NLP Model     │  │  • Kannada       │  │  • Env Vars   │ │
│  │                  │  │  • Telugu        │  │               │ │
│  │                  │  │  • Tamil         │  │               │ │
│  └──────────────────┘  └──────────────────┘  └───────────────┘ │
│                                                                   │
└───────────────────────────┬───────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│                       DATA ACCESS LAYER                          │
├─────────────────────────────────────────────────────────────────┤
│                                                                   │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │         Database Wrapper (database_sqlite.py)              │ │
│  │                                                              │ │
│  │  • MongoDB-style API                                        │ │
│  │  • SQLite Backend                                           │ │
│  │  • Query Translation                                        │ │
│  │  • Aggregation Pipeline                                     │ │
│  └────────────────────────────────────────────────────────────┘ │
│                                                                   │
└───────────────────────────┬───────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│                       DATABASE LAYER                             │
├─────────────────────────────────────────────────────────────────┤
│                                                                   │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │              SQLite Database (icgs_complaints.db)          │ │
│  │                                                              │ │
│  │  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐  │ │
│  │  │  users   │  │complaints│  │ workers  │  │ feedback │  │ │
│  │  └──────────┘  └──────────┘  └──────────┘  └──────────┘  │ │
│  │                                                              │ │
│  │  ┌──────────────────┐                                       │ │
│  │  │   dept_admins    │                                       │ │
│  │  └──────────────────┘                                       │ │
│  └────────────────────────────────────────────────────────────┘ │
│                                                                   │
└─────────────────────────────────────────────────────────────────┘
```

## 📊 Data Flow

### User Complaint Submission Flow

```
┌──────────┐
│  User    │
│ Browser  │
└────┬─────┘
     │
     │ 1. Fill complaint form
     │    (title, description, image, location)
     ▼
┌─────────────────┐
│  POST /submit   │
│   _complaint    │
└────┬────────────┘
     │
     │ 2. Validate input
     │    Upload image
     ▼
┌─────────────────┐
│  ML Prediction  │
│  (Department)   │
└────┬────────────┘
     │
     │ 3. Predict department
     │    using NLP model
     ▼
┌─────────────────┐
│  Save to DB     │
│  (complaints)   │
└────┬────────────┘
     │
     │ 4. Insert record
     │    Status: Pending
     ▼
┌─────────────────┐
│  Redirect to    │
│  Dashboard      │
└─────────────────┘
```

### Admin Worker Assignment Flow

```
┌──────────┐
│  Admin   │
│Dashboard │
└────┬─────┘
     │
     │ 1. View complaints
     │    Select complaint
     ▼
┌─────────────────┐
│  Choose Worker  │
│  from list      │
└────┬────────────┘
     │
     │ 2. POST /assign_worker
     │    (complaint_id, worker_id)
     ▼
┌─────────────────┐
│  Update DB      │
│  complaints     │
└────┬────────────┘
     │
     │ 3. Set assigned_worker_id
     │    assigned_worker_name
     │    assigned_worker_phone
     ▼
┌─────────────────┐
│  User sees      │
│  assignment     │
└─────────────────┘
```

## 🗂️ Database Schema

### Users Table
```sql
CREATE TABLE users (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    name TEXT NOT NULL,
    email TEXT UNIQUE NOT NULL,
    password TEXT NOT NULL,  -- Hashed
    role TEXT DEFAULT 'citizen',
    created_at TEXT DEFAULT CURRENT_TIMESTAMP
);
```

### Complaints Table
```sql
CREATE TABLE complaints (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    user_id TEXT NOT NULL,
    user_name TEXT,
    title TEXT NOT NULL,
    description TEXT NOT NULL,
    department TEXT,
    status TEXT DEFAULT 'Pending',
    created_at TEXT DEFAULT CURRENT_TIMESTAMP,
    image TEXT,
    location TEXT,
    latitude TEXT,
    longitude TEXT,
    assigned_worker_id TEXT,
    assigned_worker_name TEXT,
    assigned_worker_phone TEXT,
    remarks TEXT,
    admin_image TEXT
);
```

### Workers Table
```sql
CREATE TABLE workers (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    name TEXT NOT NULL,
    phone TEXT NOT NULL,
    department TEXT NOT NULL,
    created_at TEXT DEFAULT CURRENT_TIMESTAMP
);
```

### Department Admins Table
```sql
CREATE TABLE dept_admins (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    name TEXT NOT NULL,
    username TEXT UNIQUE NOT NULL,
    password TEXT NOT NULL,  -- Hashed
    department TEXT NOT NULL,
    created_at TEXT DEFAULT CURRENT_TIMESTAMP
);
```

### Feedback Table
```sql
CREATE TABLE feedback (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    user_id TEXT NOT NULL,
    title TEXT NOT NULL,
    message TEXT NOT NULL,
    image TEXT,
    rating TEXT,
    complaint_id TEXT,
    created_at TEXT DEFAULT CURRENT_TIMESTAMP,
    admin_reply TEXT,
    replied_at TEXT
);
```

## 🔐 Authentication & Authorization

### User Roles

```
┌─────────────────────────────────────────────────────────┐
│                    Role Hierarchy                        │
├─────────────────────────────────────────────────────────┤
│                                                           │
│  ┌──────────────────────────────────────────────────┐   │
│  │              Super Admin                          │   │
│  │  • View all complaints                            │   │
│  │  • Assign workers                                 │   │
│  │  • Update any status                              │   │
│  │  • View analytics                                 │   │
│  │  • Manage all departments                         │   │
│  └──────────────────────────────────────────────────┘   │
│                         │                                 │
│                         ▼                                 │
│  ┌──────────────────────────────────────────────────┐   │
│  │           Department Admin                        │   │
│  │  • View department complaints                     │   │
│  │  • Assign department workers                      │   │
│  │  • Update department complaint status             │   │
│  │  • View department analytics                      │   │
│  └──────────────────────────────────────────────────┘   │
│                         │                                 │
│                         ▼                                 │
│  ┌──────────────────────────────────────────────────┐   │
│  │              Citizen (User)                       │   │
│  │  • Submit complaints                              │   │
│  │  • View own complaints                            │   │
│  │  • Track status                                   │   │
│  │  • Provide feedback                               │   │
│  └──────────────────────────────────────────────────┘   │
│                                                           │
└─────────────────────────────────────────────────────────┘
```

## 🌐 API Endpoints

### Public Endpoints
```
GET  /                    - Home page with stats
GET  /about               - About page
GET  /contact             - Contact page
GET  /api/stats           - JSON stats (for frontend)
POST /register            - User registration
POST /login               - User login
GET  /set_language/<lang> - Change language
```

### User Endpoints (Requires Login)
```
GET  /user/dashboard      - User dashboard
POST /submit_complaint    - Submit new complaint
GET  /feedback            - View/submit feedback
POST /feedback            - Submit feedback
GET  /logout              - Logout
```

### Admin Endpoints (Requires Admin Role)
```
GET  /admin               - Admin login page
POST /admin               - Admin login
GET  /admin/dashboard     - Admin dashboard
POST /assign_worker       - Assign worker to complaint
POST /add_worker          - Add new worker
POST /update_status/<id>  - Update complaint status
POST /admin/upload_image/<id> - Upload admin image
GET  /admin/feedback      - View all feedback
POST /admin/feedback      - Reply to feedback
```

### Department Admin Endpoints
```
GET  /dept_admin/login    - Dept admin login page
POST /dept_admin/login    - Dept admin login
GET  /dept_admin/dashboard - Dept admin dashboard
POST /dept_admin/update_status/<id> - Update status
POST /dept_admin/upload_image/<id> - Upload image
POST /dept_admin/add_worker - Add worker
GET  /dept_admin/feedback - View dept feedback
```

## 🎨 Frontend Structure

```
templates/
├── home.html              - Landing page
├── login.html             - User login
├── register.html          - User registration
├── user_dashboard.html    - User dashboard
├── admin_login.html       - Admin login
├── admin_dashboard.html   - Admin dashboard
├── dept_admin_login.html  - Dept admin login
├── dept_admin_dashboard.html - Dept admin dashboard
├── department_dashboard.html - Department view
├── feedback.html          - User feedback
├── admin_feedback.html    - Admin feedback view
├── about.html             - About page
├── contact.html           - Contact page
├── header.html            - Common header
└── footer.html            - Common footer

static/
├── style.css              - Main stylesheet
├── images/                - Images and logos
└── uploads/               - User uploaded files

netlify-frontend/          - Static frontend (optional)
├── index.html             - Static home page
├── js/
│   └── main.js            - Frontend JavaScript
└── netlify.toml           - Netlify config
```

## 🔄 Request/Response Cycle

```
1. Browser Request
   ↓
2. Flask Router (app.py)
   ↓
3. Authentication Check (session)
   ↓
4. Route Handler Function
   ↓
5. Business Logic (ML, validation)
   ↓
6. Database Query (database_sqlite.py)
   ↓
7. SQLite Database
   ↓
8. Data Processing
   ↓
9. Template Rendering (Jinja2)
   ↓
10. HTML Response
    ↓
11. Browser Display
```

## 🚀 Deployment Architecture

### Local Development
```
┌─────────────────┐
│  Developer PC   │
│                 │
│  Flask Dev      │
│  Server         │
│  (port 5000)    │
│                 │
│  SQLite DB      │
│  (local file)   │
└─────────────────┘
```

### Production (Render/Heroku)
```
┌──────────────────────────────────────┐
│         Cloud Platform               │
│  (Render/Heroku/Railway)             │
│                                      │
│  ┌────────────────────────────────┐ │
│  │  Gunicorn WSGI Server          │ │
│  │  (Multiple Workers)            │ │
│  │                                │ │
│  │  ┌──────────────────────────┐ │ │
│  │  │  Flask Application       │ │ │
│  │  └──────────────────────────┘ │ │
│  └────────────────────────────────┘ │
│                                      │
│  ┌────────────────────────────────┐ │
│  │  Persistent Storage            │ │
│  │  (SQLite or PostgreSQL)        │ │
│  └────────────────────────────────┘ │
│                                      │
│  ┌────────────────────────────────┐ │
│  │  Static Files                  │ │
│  │  (CDN or Local)                │ │
│  └────────────────────────────────┘ │
└──────────────────────────────────────┘
         │
         ▼
┌──────────────────┐
│  Load Balancer   │
│  (HTTPS/SSL)     │
└──────────────────┘
         │
         ▼
┌──────────────────┐
│  End Users       │
└──────────────────┘
```

## 📦 Dependencies

### Core Dependencies
- **Flask 3.0.3** - Web framework
- **Werkzeug 3.0.3** - WSGI utilities
- **Flask-Babel 4.0.0** - Internationalization
- **Gunicorn 21.2.0** - Production WSGI server

### ML Dependencies (Optional)
- **scikit-learn 1.3.0** - Machine learning
- **transformers 4.30.0** - NLP models
- **torch 2.0.0** - Deep learning
- **numpy <2.0.0** - Numerical computing

### Database
- **SQLite3** - Built-in with Python
- **pymongo 4.6.1** - MongoDB driver (for future migration)

## 🔧 Configuration

### Environment Variables
```bash
SECRET_KEY=your_secret_key
PORT=5000
FLASK_ENV=production
```

### Config Files
- `.env` - Environment variables
- `config.py` - Application configuration
- `render.yaml` - Render deployment config
- `Procfile` - Heroku deployment config
- `runtime.txt` - Python version specification

---

**Last Updated**: 2025-11-27
**Version**: 1.0.0
